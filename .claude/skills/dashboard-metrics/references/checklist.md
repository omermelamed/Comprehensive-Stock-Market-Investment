# Dashboard metric checklist

- What is the authoritative source for this metric?
- Is the calculation deterministic?
- Does the backend expose the value clearly?
- Does the UI show loading, empty, and error states?
- Does the status color mean the same thing everywhere?
