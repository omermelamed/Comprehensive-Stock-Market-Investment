---
argument-hint: [frontend task]
description: Implement a frontend task with the frontend-agent, React and Tailwind rules, and targeted frontend validation.
---

Use the `frontend-agent` subagent for this task:

$ARGUMENTS

## Required process

1. Read `@CLAUDE.md` and `@frontend/CLAUDE.md` first.
2. Use relevant skills such as `react-vite-tailwind`, `monthly-investment-flow`, `dashboard-metrics`, or `api-contract-sync`.
3. If the UI needs backend contract changes, coordinate with `backend-agent` and `api-contract-reviewer`.
4. Keep deterministic values visually primary over AI text.
5. Run the smallest relevant frontend checks before finishing.
6. Report files changed, commands run, results, and any follow-up work.
