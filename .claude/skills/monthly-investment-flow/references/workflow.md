# Monthly flow workflow

Preview phase:
- accept budget
- compute suggestions
- return editable card models

Confirm phase:
- validate totals
- log resulting BUY transactions
- return confirmation summary

Keep preview and confirm separate so the UI can iterate without writes.
