# API contract checklist

- Did backend DTOs change deliberately?
- Did frontend API types update in the same pass?
- Is the UI missing any fields that would force duplicated calculations?
- Are enum names and optional fields explicit?
- Were at least minimal checks run on both touched layers?
