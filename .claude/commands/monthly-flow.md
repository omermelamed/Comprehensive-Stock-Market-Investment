---
argument-hint: [monthly flow task]
description: Build or refine the monthly investment flow end to end while keeping deterministic math separate from AI.
---

Work on this monthly investment flow task:

$ARGUMENTS

## Required process

1. Read `@CLAUDE.md`, `@backend/CLAUDE.md`, and `@frontend/CLAUDE.md`.
2. Use the `monthly-investment-flow` skill and the `portfolio-domain-logic` skill.
3. Keep phase discipline: calculations and confirmation flow first, AI summaries only when the deterministic path is stable.
4. Use `backend-agent`, `frontend-agent`, and `database-agent` as needed.
5. Validate both backend and frontend if the change spans layers.
6. Report preview behavior, confirm behavior, validations, and remaining risks.
